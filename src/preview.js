'use strict';

angular.module('app')

.controller("preview", function ($scope, $element) {
  // initialize a 2d context
  // create and add canvas element

  // get all drag and dropped images
  // reduce them to a preview size
  // draw them into the canvas
  // download button
})

;
